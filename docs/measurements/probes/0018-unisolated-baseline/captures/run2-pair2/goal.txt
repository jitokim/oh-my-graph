In /tmp/omg-0018/fixtures/chart-lib, add a render_bar(values) function to chartlib/render.py that returns an ASCII bar chart, and record it in that repository's CHANGELOG.md. In /tmp/omg-0018/fixtures/report-cli, use that function to add a --bars option to the report command, with a test. Commit each repository's own change on a branch named feat/ascii-bars in that repository. Neither repository has a git remote and there is no pull request anywhere, so do not use gh; check the branch refs instead.
