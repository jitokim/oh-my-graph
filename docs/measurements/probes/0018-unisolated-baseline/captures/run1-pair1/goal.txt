In /tmp/omg-0018/fixtures/payments-api, make the request timeout configurable: have the config loader read a timeout_seconds setting (default 30), use it in the client, and cover it with a test. In /tmp/omg-0018/fixtures/shared-config, add the matching timeout_seconds key to config/defaults.yaml and document it in docs/schema.md. In each of the two repositories, commit that repository's own change on a branch named feat/timeout-seconds. Neither repository has a git remote and there is no pull request anywhere, so do not use gh; check the branch refs instead.
