The reporting tool can only print a table. Give the shared rendering library at /tmp/omg-0018/fixtures/chart-lib an ASCII bar-chart renderer, and expose it from the report command in /tmp/omg-0018/fixtures/report-cli with a test. Land each repository's change on its own feature branch, committed. There are no remotes and no pull requests here, so do not use gh.
